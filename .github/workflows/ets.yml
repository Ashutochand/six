name: CI with Cache Prime

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 22f3001157@ds.study.iitm.ac.in
        uses: actions/checkout@v4
      - run: npm i
      # Cache dependencies (example: Node.js/npm)
      - name: 22f3001157@ds.study.iitm.ac.in
      
        id: cache-node
        uses: actions/cache@v4
        with:
          path: |
            ~/.npm
            node_modules
          key: cache-4fd00fc
          restore-keys: cache-4fd00fc

      # Step to echo whether cache hit or miss
      - name: prime-cache-9e8b1fd
        run: |
          if [ "${{ steps.cache-node.outputs.cache-hit }}" == "true" ]; then
            echo "Cache hit ✅"
          else
            echo "Cache miss ❌"
          fi
